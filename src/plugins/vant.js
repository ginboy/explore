import { Button,Popup,Cell,Field,Cascader,Picker,Slider,Circle}from "vant";
export default (app) => {
  app.use(Button);
  app.use(Popup);
  app.use(Cell);
  app.use(Field);
  app.use(Cascader);
  app.use(Picker);
  app.use(Slider);
  app.use(Circle)
};

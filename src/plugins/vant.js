import { Button,Popup,Cell,Field,Cascader,Picker}from "vant";
export default (app) => {
  app.use(Button);
  app.use(Popup);
  app.use(Cell);
  app.use(Field);
  app.use(Cascader);
  app.use(Picker);
};
